package gr.aueb.cf.schoolapp.model;

/**
 * Enumeration representing the roles a user can have in the school application.
 * The roles define the different levels of access and permissions for users.
 */
public enum Role {
    TEACHER,
    STUDENT,
    ADMIN
}
